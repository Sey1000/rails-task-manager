<h2>Nhung's To do list</h2>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task_path(task) %>
    <%= link_to 'edit', edit_task_path(task) %>
    <%= link_to 'delete', task_path(task), method: :delete %>
  </li>
  <% end %>
</ul>
<%= link_to 'New Task', new_task_path %>